name: module_base
description: "ZotPaper公共基类"

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
#
# This version is used _only_ for the Runner app, which is used if you just do
# a `flutter run` or a `flutter make-host-app-editable`. It has no impact
# on any other native host app that you embed your Flutter project into.
version: 1.0.0+1

environment:
  sdk: '>=3.4.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # provider
  provider: ^6.1.0  # 使用最新版本

  # flutter_boost
  flutter_boost:
    git:
      url: 'https://github.com/alibaba/flutter_boost.git'
      ref: '5.0.2'

#  webdav_client:
#    git:
#      ref: v1.0.66
#      url: https://github.com/lollipopkit/webdav_client

  # 多语言支持
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

  #  fl_lib:
  #    path: ../fl_lib

  #  fl_lib:
  #    git:
  #      url: https://github.com/lppcg/fl_lib
  #      ref: v1.0.209

  icons_plus: ^5.0.0  # 使用最新版本（请检查 pub.dev 获取最新版本号）

  flutter_markdown: ^0.7.1

  url_launcher: ^6.2.6

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  shared_preferences: ^2.2.3

  logging: ^1.2.0

  flutter_dotenv: ^5.1.0  # 最新版本

  # webview
  webview_flutter: ^4.0.0
  # OAuth1
  oauth1: ^2.0.0
  # dio
  dio: ^5.4.0

  json_annotation: ^4.9.0
  json_serializable: ^6.8.0

  pretty_dio_logger: ^1.2.0  # 请确保使用最新版本

  path: ^1.8.3

  flutter_svg: ^2.0.7  # svg

  # 获取储存路径
  path_provider: ^2.0.15

  event_bus: ^2.0.1

  # Bruno UI组件库
  #  bruno: ^3.4.3
  bruno:
    #    path: /Users/moyear/StudioProjects/bruno-moyear
    git:
      url: https://github.com/moyearzhou/bruno-moyear.git
  #      ref: v3.4.3

  # bugly
  flutter_bugly: ^1.1.0

  # supabase
  supabase_flutter: ^2.10.3

  # device_info
  device_info_plus: ^11.1.0

  # package_info
  package_info_plus: ^8.1.0

  # connectivity
  connectivity_plus: ^5.0.0

  # uuid
  uuid: ^4.5.1

  #  webdav_client: ^1.2.2
  webdav_client:
#    path: /Users/moyearzhou/StudioProjects/webdav_client2
    git:
      url: git@github.com:moyearzhou/webdav_client2.git

  open_filex: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  assets:
    - assets/
    - assets/image/
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add Flutter specific assets to your application, add an assets section,
  # like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add Flutter specific custom fonts to your application, add a fonts
  # section here, in this "flutter" section. Each entry in this list should
  # have a "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


  # This section identifies your Flutter project as a module meant for
  # embedding in a native host app.  These identifiers should _not_ ordinarily
  # be changed after generation - they are used to ensure that the tooling can
  # maintain consistency when adding or modifying assets and plugins.
  # They also do not have any bearing on your native host application's
  # identifiers, which may be completely independent or the same as these.
  module:
    androidX: true
    androidPackage: com.moyear.zotpaper.base.module_base
    iosBundleIdentifier: com.moyear.zotpaper.base.moduleBase
